{
  "name": "opencode-cockpit-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "bun run clean:daemons && bun run build && bun test --conditions browser",
    "typecheck": "tsc -b",
    "lint": "biome check .",
    "format": "biome format --write .",
    "lint:fix": "biome check --write .",
    "check": "bun run clean:daemons && bun run build && bun run lint && bun run typecheck && bun test --conditions browser",
    "pack:check": "bun scripts/pack-check.ts",
    "version:set": "bun scripts/set-version.ts",
    "build": "bun scripts/build.ts",
    "smoke:tui": "bun scripts/tui-smoke.ts",
    "release": "bun scripts/release.ts",
    "record": "bun scripts/record.ts",
    "clean:daemons": "bun scripts/clean-daemons.ts",
    "preview": "bun packages/status/src/cli/preview.ts",
    "capture": "bun scripts/capture.ts"
  },
  "devDependencies": {
    "@babel/core": "7.28.0",
    "@babel/preset-typescript": "7.27.1",
    "@biomejs/biome": "2.5.13",
    "@types/bun": "^1.4.2",
    "@xterm/addon-serialize": "^0.14.0",
    "@xterm/headless": "6.0.0",
    "babel-preset-solid": "1.9.12",
    "typescript": "7.0.2"
  },
  "version": "0.5.0",
  "engines": {
    "bun": ">=1.3.5"
  }
}
